{
  "name": "svgoconfig",
  "version": "1.0.0",
  "scripts": {
    "start": "svgo --config=config.yml -f ./input -o ./output; ls -lh output",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "blopker",
  "license": "MIT",
  "dependencies": {
    "svgo": "^0.5.6"
  }
}
